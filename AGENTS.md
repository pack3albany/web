# Agent Instructions

## Git workflow

Commit and push changes directly to `main`. Do not open pull requests.

Pushing to `main` with changes under `site/` triggers the GitHub Pages deploy workflow (`.github/workflows/pages.yml`).
